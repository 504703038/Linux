package Bussiness;

/*
 * 功能：重置密码
 */

import Common.*;

public class ResetPassword {

	public ResetPassword(User user) 
	{
		/*
		 * if (手机号匹配)
		 * if (两次密码不一样)
		 * {	 
		 * 		弹出提示框"两次密码不一致";
		 * 		title = new JLabel("两次密码不一致");
		 * 		title.setFont(new Font("微软雅黑",Font.PLAIN,16));
		 * 		JOptionPane.showMessageDialog(null, title,"提示", 2);
		 * 		return;
		 * }
		 * else
		 * {
		 * 		数据库中的密码=newpassword.getTEXT();
		 * 		title = new JLabel("重置密码成功");
		 * 		title.setFont(new Font("微软雅黑",Font.PLAIN,16));
		 * 		JOptionPane.showMessageDialog(null, title,"提示", 2);
		 * 		弹出提示框"重置密码成功";
		 * 		dispose();
		 * }
		 * else 
		 * {	弹出提示框"手机号与注册时所用手机号不匹配";
		 * 		title = new JLabel("手机号与注册时所用手机号不匹配");
		 * 		title.setFont(new Font("微软雅黑",Font.PLAIN,16));
		 * 		JOptionPane.showMessageDialog(null, title,"提示", 2);
		 * 		return;
		 * }
		 * 
		 */
	}

}
